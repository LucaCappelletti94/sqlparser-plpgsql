
BEGIN
    IF NEW.event_type = 'error' THEN
        INSERT INTO audit_log (aTO audit_log vent_type = 'info' THEN
        INSERT INTO audit_log (action, severity) VALUES ('info_event', 'low');
    ELSE
        INSERT INTO audit_log (actiW;
END;
