
BEGIN
    IF NEW.event_type = 'error' THEN
        INSERT IN
BEGIN
	INSERT INTO owners (id) VALUES (NEW.id);
	RETURN NEW;
END;
       INSERT INTO audit_log (action, severit      INSERT INTO audit_log (action, severity) VALUES ('ELSIF NEW.event_type = 'warning' THEN
        INSERT INTO audit_log (action, severit      INSERT INTO audit_log (action, severity) VALUES ('warning_event', 'medium');
   ELSIF NEW.event_type = 'N NEW;
END;
