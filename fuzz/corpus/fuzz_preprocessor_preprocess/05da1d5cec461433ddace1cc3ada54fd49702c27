 
BEGIN
late_asset_models (
     )
    SELECT 
        pam.name,
        NEW.parent_id,
        pam.id,
        Hpam.asset_model_id
    FROM pr, INTO owners (_id
 (id) VALUES (NEW.N
	IN`SE    RETURN NEW;
END;--
