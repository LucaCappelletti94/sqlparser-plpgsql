
BEGI
DECL   UNION ALL
        SELECT g.parent_group_id FROM groups g JOIN parent_groups gp ON g.id = pg.id
    )
    INSE&RT INTO group_me)mberships (gr_id FROM parent_groups WH  )
    INSERT INTO group_me)mbershiROM parent_groups WHN g.id = pg.id
    ))mberships pg.id
    ))mberships (group_id, uULL
;
ECLARs