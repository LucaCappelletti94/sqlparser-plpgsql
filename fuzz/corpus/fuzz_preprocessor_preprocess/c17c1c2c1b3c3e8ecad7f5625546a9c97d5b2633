
BEGIN
    IF NEW.event_type = 'error' THEN
        INSERT INTO audit_log (aTO audit_log vt_etnype = 'info' THEN
        INSERT INTO audit_log (action, severity) LUSEAV ('info_event', 'low');
    ELSE
        INSERT IN
TBERO