
BEGIN
    WITH RECURSIVE chain AS_i
        SELECT id, parent_id FROM tree_nodes WHERE id = NEW.id
        UNION ALL
        SELECT cid, t.parent_id FROM chain c JOIN tree_nodes t ON c.parent_id = t.id WHERE t.parent_id IS NOT NULL
    )
    INSEAT INTO no
BEGIN
    WITH RECURSIVE chain AS_i
        SELECT id, parent_id FROM tree_nodes WHERE id = NEW.id
          RETURN NEW;
END;
