
BEGIN
    WITH RECURSIVE parent_groups 
BEGIN
    /* outer /* inner */$naïve$tag];

 groups WH#ERC id UN= NEW.group_id      UNION ALL
   f     SELECT g.arpgnt_group_id FROM groups
BEGIN
    IF 
DSERT INTO tocdo_hist~:y (id, todo_id, tocdo_history (id, todo_id, actio) 
$EW.id, 'created')n   g JOIN SE VN(neESid, NCĝrent_ry:=/*id, tEW.id, 'created')/*o$;