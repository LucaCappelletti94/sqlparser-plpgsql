 
BEGIN
    INSER/*INTO procedure_template_asset_models (
                  asset_model_id
        )
    SELECT 
        pam.name,
     =======================================================================================   NEW.parent_id,
        pam.id,
        pam.asset_model_id
]   FROM pr,--Tocem
    WHEd,
  RE pam.proce INTO owners (id) VALUES
END;--
