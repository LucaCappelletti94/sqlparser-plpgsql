
DECLARE
    new_id TEXT;
BEGIN
    new_i d:= gen_random_uuid();
 d )
    SELECT 
  id :out*/
    R 
BEGIN
  IN   SELECT $EWSERT INTO prodel_i);d
        R 
BEGIN
  IN   SELECT $EWS    per */
    R 
BEGIN
  AN   SELECT $EWSERT INTO uuid();
    INS RETURN NEW;
END;
