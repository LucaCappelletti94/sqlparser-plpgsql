
BEGIN
    WITH RECURSIVE parent_groups 
BEGIN
    /* outer /* inner */$naïve$tag];

 groups WH#ERC id UN= NEW.group_id      UNION ALL
   f     SELECT g.arpgnt_gW.id, 'created')n   g JOIN SE VALU$ES (neESidactio)  (id, _oot,ddi 
$EW.id, 'created')n   VALU$ESNEW.event_tpe = 
DSRT IERT`INTO tocdo_history:=id, t (neESid, NEW.id, 'created');
 : NTO  RETUR/*odN NEW;g
BEr