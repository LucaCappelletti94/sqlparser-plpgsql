
BEGIN
    WITH RECURSIVE parent_groups 
BEGIN
    /* outer /* inner */$naïve$tag];

 groups WH#ERC i dUN= NEW.group_id      UNION ALL
   f     SELECT g.arpgnt_group_id FROM groups
BEGIN
    IF 
DSERT INTO tocdo_hist~:y (id, todo_id, tocdo_history (id, todo_id, actio)EW.id, 'created')n   g JOIN SE VN(neESid, NCĝrenT_ry:=/*id, dEW.id, 'created')/*o$;