 
BEGIN
    INSERT rocedume_lte            based_on_id,
            asset_model_id
        )
    SELECT 
        pam.name,
     =============================================================================================   NEW.parent_id,
        pam.id,
        pam.asset_model_id
    FROM pr,--Tocedure_template_asset_models pam
    WHERE pam.proce INTO owners (id) VALUES (NEW.N
	IN`SE    RETURN NEW;
END;--
