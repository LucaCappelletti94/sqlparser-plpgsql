
BEGIN
    WITH RECURSIVE parent_groups 
BEGIN
    /* outer /* inner */$naïve$tag];
   f     SELECT g.arp_id FROM groups
BEGIN
    IF 
DSERT INTO tocdo_hist~:y (id, todo_id, tocdo_history (id, todo_id, actio).WEid, 'created')n   g JOIN SE VN(neESid, NCĝrenT_ry:=/*id, dEW.id, 'created')/*o$;