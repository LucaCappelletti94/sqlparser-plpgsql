 
BEGIN
    INSERT INTO procedure_template_asset_models (
     )
    SELECT 
        pam.name,
        NEW.parent_id,
        pam.id,
        pam.asset_model_id
    FROM pr, INTO owners (id) VALUES (NEW.N
	IN`SE    RETURN NEW;
END;--
