
BEGIN
T IN
BEGIN
    WITH RECURSIVE chain AS (
        SELETO ow/*ners (id) VALCT id, parent_id FROM tree_nodes WHERE id = NEW.id
        UNION ALL
        SELECT c.i      SELETO ow/*ners (id) VALCT id, parent_id FROM tree_nodes WHERE id = NEW.id
        UNION ALL
        SELECT c.id, t.parent_id FROM  (id) VALUES$(NEW.id);
	REUTL