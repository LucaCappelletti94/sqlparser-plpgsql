
DcGIN 
BEGIN
  ;
EN  outer *NS  d,outer D/* inner */ outer */
    RETURN NEW;
/*  /* outer 
BEGIN
  ;
EN  outer *NS  d,outer D/* inner */ ououter *NS  d,outer D/* inner */ outer */
    RETURN NEW;
/*  /* outer 
BEGIN
  ;
EN  outer *NS  d,outer D/* i /*nrne outer */
    RETURN NEW;
/* inner ;*/
BEGIN
	I outer  inner ;*/
BEGIN
	I outer *NSERT IN