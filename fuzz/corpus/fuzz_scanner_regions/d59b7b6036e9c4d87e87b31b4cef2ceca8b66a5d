
BE--N
	INSERT I/*O  */ outer */
    RETURN NEW;
/* inner */
BEGIN
	I outer *NSERT INTO oowners (id) VALUES (NEW.EN        INSERT INTO audit_log (acGIN
    /* outer 
BEGIN
    d, outer  /* outer /* inner */"outer */
    RETURN NEW;*/
BEGIN
	I outer *NSERT INTO owit_lo;
g (action, 