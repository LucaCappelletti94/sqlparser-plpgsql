
BEGIN
    WITH RECURSIVE chain AS (
        SELECT id, parent_id FROM tre 
BEGIN
    INSERT INTO procedure_template_asset_mo
BEGIN
    IF NEW.event_type = 'error' THEN
        INSERT INTO audit_log (action, severity) VALUES ('error_eERE id = NEW.id
        UNIset_model_id
        )
 "  SELECT 
        pam.name,
        NEW.parent_id,
        pam.id,
        pam.asset_model_id
    FROM proceON ALL
       dure_ SELECT c.id, t.parent_id FROM chtemplate_asse