
BEGIN
    IF NEW.event_type = 'error' THEN
        INSER
DSIF NEW.event_type = 'warning' THEN
        INSERT INTO audit_log (naïve$tag];
BENIG
    -- Grüaction, severiße 世界
    RAISE NOTICE '%'ty, c)