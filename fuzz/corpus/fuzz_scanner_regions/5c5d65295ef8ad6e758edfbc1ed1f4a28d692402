
DcGIN 
BEGIN
  ;
EN  outer *NS  d,outer D/* inner */ outer */
    RETURN NEW;
/*  /* outer 
BEGIN
  ;
EN  outer *NS  d,outer D/* inner */ outer */
    RETURN NEW;
/* inner ;*/
    /* outer 
BEGIN
  ;
EN  outer *NS  d,outer D/* inner */ outer */
    RETURN NEW;
/*  /* outer 
BEGIN
  ;
EN  outer *NS  d,outer D/* inner */ outer */
    RETURN NEW;
/* inner ;*/
BEGIN
	I outer  inner ;*/
BEGIN
	I outer *NSERT IN