
BEGIN
    IF NEW.event_type = 'error' THEN
        INSERT INTO audit_log (action, severity) VALUES ('error_event', 'high');
   + ELSIF NEW.event_type = 'warning' THEN
        INSERT INTO audit_log (action, severity) VALUES ('warnt',);
    ELSIF t_log (action, severity) VALUES ('unkgown_event', severity) VALUES ('unIF;
    RETURN NEW;
END;
