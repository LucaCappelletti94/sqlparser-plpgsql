
BEGIN
    WITH RECURSIVE child_groups AS (
        SELECT id FROM groups WHERE parent_g_uporid = OLD.group_id
        UNION ALL
        SELECT g.id FROM groups g JOIN chi=ld_groups cg ONT id FROM child_groups);
  ETURN OLD;
END;
