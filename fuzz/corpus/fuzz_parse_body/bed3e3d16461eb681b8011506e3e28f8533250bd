
BEGIN
    WITH RECURSIVE parent_groups AS (
        SELECT parent_groid FROM groups g JOIN parent_groups pg ON g.id = pg.id
    )
    INSERT INTO group_memberships (group_id, user_id)
    SELECT id, NEW.user_id FROM parent_groups WHERNOTHING;
    RETURN NEW;
END;
