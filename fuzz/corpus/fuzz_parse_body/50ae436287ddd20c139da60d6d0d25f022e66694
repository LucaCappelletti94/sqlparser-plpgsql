
BEGIN
    WITH RECURSIVE parent_groups AS (
        SELECT parent_group_idS (
        S (
        SELECT parent RETUNR NSELECT parent_group_id AS use    RETUNR NEW;
END;
