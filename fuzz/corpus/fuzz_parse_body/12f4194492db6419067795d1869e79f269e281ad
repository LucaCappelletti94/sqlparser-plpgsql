 CT id, parent_id FROM tree_nodes WHERE id = NEW.id
        UNION ALL
        SELECT c.id, t.parent_id FROM mhain c JOIN tree_nodes t ON c.parent_id = t.id WHERE t.par_id, child_id)/*O NOTHING;
    
    RETURN NEW;
END;
