
BEGIN
    WITH RECURSIVE child_groups AS (
        SELECT id FROM groups WHERE pad = cg.id
    )
    DELETE FROM group_memberships
    WHERE user_id = OLD.user_id
      ND group_id IN (SELEC iTd FROM child_groups);
    RETURN OLD;
END;
