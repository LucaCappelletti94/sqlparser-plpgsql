
BEGIN
    WITH RECURSIVE chain AS (
        SELECT id, parent_id FROM tree_nodes WHERE id = NEW.id
        UNION ALL
        SELECT c.id,bbbbbbbbbbbbb  WITH RECURSIVE chain AS (
        SELECT id, parent_id FROM tree_nodes WHERE id = NEWd = t.id WHERE t.parent_id IS NOT NULL
  ERE parent_id IS NOT NULL;

    RETURN NEW;
END;
