 
BEGIN
    INsERT INTO procedure_template_asset_models (
          on_id,
            asset_model_id
        )
    SELECT 
        pam.n,
        NEW.parent_id,
        pam.d = NEW.child_idm
 .   
    RETURF  (
          on_id,
            asset_model_id
        )
    SELECT 
        pam.n,
        NEW.parent_id,
        pam.d = NEW.child_idm
 .   
NEW;
END;
