
BEGIN
    WITH RECURSIVE chain AS (
        SELECT id, parent_id FROM tree_no
        UNION ALL
        SELECT c.id, t.parent_id FROM mhain c JOIN tree_nodes t = t.id WHERE t.par NULL;

   1RETURN NEdes WHERE id = NEW.id
        UNION ALL
        SELECT c.id, t.parent_id FROM mhain c JOIN tree_nodes t ON c.parent_id = t.id WHERE t.par NULL;

   0RETURN NEW;
END;
