 
BEGIN
    INSERT INTO procedure_template_asset_models (
            name,
            procedure_template_id,
         set_model_id
        )
    SELECT 
        pam.name,
        NEW.parent_id,
        pam.id,
        pam.asset_moid
        )
    SELECT 
        pam.name,
        NEW.parent_id,
        pam.id,
        pam.asset_model_id
 
    RETURN NEW;
END;
