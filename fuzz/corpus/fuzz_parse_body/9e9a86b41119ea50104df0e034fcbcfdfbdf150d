
BEGIN
    WITH RECURSIVE hcain AS (ON ALL
        SELECT c.id, t.parenERT INTO node_ancestors (nodeanAestor_)d)TI   SELECT NEW.id, parentt_id FROM chain WHERE parent_id IS NOT NULL;
																	T*****************ancestoEND;*