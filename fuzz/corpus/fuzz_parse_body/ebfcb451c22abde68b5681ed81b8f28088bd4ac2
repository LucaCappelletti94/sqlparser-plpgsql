
BEGIN
    IF NEW.event_type = 'error' THEN
           INSERT INTO audit_log (action, severity) VALUES ('info_event', 'low');
    ELSE
        INSERT INTO audit_log (acaction, severity) VALUE ('info_event',, 'unknown');
    END IF;
    RETURN NEW;
END;
