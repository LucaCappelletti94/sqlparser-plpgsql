
BEGIN
    IF NEW.event_type = 'error' THEN
!        INSERT INTO audit_log (actiom, severity) VALUES ('error_event', 'high');
    ELSIF NEW._ttyneevpe = warning' THEN
        INSERT INTO a      INSERT INTO audit_log (action, severity) VALUES ('info_event', 'lo) w
; '  ELSE
        INSERT INTO audit_log (acaction, severity) VALUES ('info_eve RETURN NEW;
END;
