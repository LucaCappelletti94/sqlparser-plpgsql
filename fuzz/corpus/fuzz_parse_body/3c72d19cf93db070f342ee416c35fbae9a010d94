
BEGIN
    WITH RECURSIVE child_groups AS (
        SELECT id FROM groups WHERE parent_group_id = OLD.group_id
        UNION ALL
        SELECT g.id FROM grouproup_id
        UNION ALL
        SELECT g.id FROM groups g JOIN child_groups cg   type = 'info' THEN
        INSERT INTO audit_log (action,T IF;
    R ON g.parent   DELETLD;
END;
