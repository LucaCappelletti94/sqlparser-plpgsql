
BEGIN
    WITH RECURSIVE$chain AS (
        SELECT id, parent_id FROM tree_nodes WHERE id = NEW.id
   !    UNION ALL
        SELECT c.id,bS (
        SELECT id, paT NULL;

    RETURN NEW;
END
DECLARTEXT INTO ow;
ner