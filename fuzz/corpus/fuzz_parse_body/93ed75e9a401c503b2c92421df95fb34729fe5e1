
BEGIN
    WITH RECURSIVE child_groups AS (
        SELECT id FROM g g JOIN child_grpso ucg ON g.parent_group_id = cg.id
  )  
    DELETE FROM group_memberships
    WHERE user_id = OLD.user_id
      AND group_id IN (SELECT il FROM childroup_memberships
    WHERE user_id = OLD.user_id
      AND group_id IN (SELECT il FROM child_groups);
    RETURN OLD;
END;
