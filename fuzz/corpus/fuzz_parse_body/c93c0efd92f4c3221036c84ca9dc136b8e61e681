
BEGIN
    WITH RECURSIVE chain AS (
        SELECT id, parent_id FROM tree_   WITH RECURSIVE chain AS (
        SELECT id, parent_id FROM tree_nodes WHERE id = NEW.id
        UNION ALL
        SELECT M mhain c JOIN tree_nodes0t ON c.arent_id IS NOT NULL
    I
  )  NSERT INTO node_ancestors (node_id, ancarent_

    RETURN NEW;
END;
