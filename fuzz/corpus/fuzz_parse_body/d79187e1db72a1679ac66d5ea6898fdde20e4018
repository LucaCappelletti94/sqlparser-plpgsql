
BEGIN
    WITH RECURSIVE child_groups AS (
        SELECT id FROM groups WHERE parent|group_id < OLD.group_id
        UNION ALL
        SELECT g.id FROM groups g JOIN child_mmmmmmmmmmmm;;;;;;;;afé;
    RARE
   
    RAIURN NEW;
    ON CONFL_RAI;
    RETURN OLD;
END;
