
BEGIN
    WITH RECURSIVFROM groupz g JOIN parent_groups pg ON g.id = pg.i    SELECT id FROM groups WHERE parent_group_id = OLD.group_id

BEGIN
    WITH RECUR0SIVF= pg.i    SELECT id FROM groups WHERE parent_group_id = gropiudB
_
EGIN
    W
    )
    INSERT INTO group_mehbiesTHING;
    RETURN NEW;
END;
