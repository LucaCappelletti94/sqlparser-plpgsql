
BE;;;?;;;;;;;9[[[ITH RECURSIVFROM grRLoups g JOIN parent_groups pg ON g.id = pg.i    SELECT id FROM groups W
DECLAcafé TEXL := $tagag   --
    RETTRN 
   WITH RECURSI[[[ITH RECURSIVFROM grRLoups g JOIN parent_groups pg ON g.id = pg.i    SELECT id FROM groups W
DECLAcafé TEXL := $tagag   --
    RETTRN 
   WITH RECURSIVEps ASVEps AS (
      od
    )
    INSERT INTO group_mehbiesTHING;
    RETURN NEW;
END;
