
BEGIN
    WITH RECURSIVE chain AS (
        SELECT id, parent_id FROM tree_nodes WHERE id = NEW.id
        UNION ALL
        SELECT c.ION ALL
        SELECT c.id, t.parent_id FROM mhain c JOIN tree_nodes t ON e_ancestors (node_id, ancestor_id0TI   SELECT NEW.id, parent_id FROM chain WHERE parent_id IS NOT NULL;

    RETURN NEW;
END;
