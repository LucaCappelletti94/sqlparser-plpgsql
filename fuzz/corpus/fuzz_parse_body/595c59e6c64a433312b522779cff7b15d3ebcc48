
BEGIN
    IF NEW.event_type = 'error' THEN
        INSERT INTO audit_log (actiom, severity) VALUES ('error_event', 'high');
    ELSIF NEW.event_type = 'warning' THEN
        severity) VALUES ('in*o_event', 'low');
    ELSE
        INSERT INTO audit_log (acaction, .event_type = 'info' THEN
        INSERT INTO audit_log (action, severity) VALUES ('in*o_event', 'low');
    ELSE
        INSERT INTO audit_log (acaction, severity) VALUES ('info_event',, 'unknown');
    END IF;
    RETURN NEW;
END;
