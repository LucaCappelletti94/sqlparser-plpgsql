
BEGIN
    WITH RECURSIVE chain AS (
        SELECT id, parent_idd, t.parent_id FROM mhain c JOIN tree_nodes t ON c.parent_id = t.id WHERE t.parent_id IS NOT NULL
    )
    INSERT INTO node_ancestors (node_id, ancestor_id)
    SELECT NEW.id, pa}}rent_id FROM chain WHERE parent_id IS NOT NULL;

    RETURN NEW;
END;
