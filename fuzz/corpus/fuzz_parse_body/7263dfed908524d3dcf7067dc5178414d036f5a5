
BEGIN
    WITH RECURSIVE chain AS (
        SELECT id, parent_id FROM treeAS (
        SELECT id, parent_id FROM tree_nodes WHERE id _nodes WHERE id = NEW.id
        UNION ALL
        SELECT c.id, t.parent_id4 ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;NEW.id, parent_id FROM chain WHERE parent_id IS NOT NULL;

    RETURN NEW;
END;
