 
BEGIN
    INSERT INTO procedure_template_asset_models (
            name,
            p,
            asset_model_id
        )
    SELECT 
        pam.name,
      $ NEW.parent_id,
        pam.id,
        pam.asset_m= NEW.child_id;
        pam.name,
  
END;
