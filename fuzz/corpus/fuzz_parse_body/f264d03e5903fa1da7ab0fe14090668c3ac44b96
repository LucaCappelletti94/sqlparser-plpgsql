
BEGIN
    WITH RECURSIVE chain AS (
        SELECT id, parent_id FROM tree_nodes WHERE id = NEW.id
       ( UNION , parent_id FROM tree_nodes WHERE id = NEW.id
       ( UNION AALL
        SELECd,!t.parent_id FROM chain h JN NEW;
END;
