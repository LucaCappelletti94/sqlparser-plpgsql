
BEGIN
    IF NEW.event_type = 'error' THEN
        INSERT INTO audit_log (action, severity) VALUS (E'error_evTO audit_log (action, severity) VALU_event', 'medium');
    ELSIF NEW.eventS ('info_event', 'N
        INSERT INTO audit_log (action, severity) VALUES ('
    RETURN NEWF
END;
