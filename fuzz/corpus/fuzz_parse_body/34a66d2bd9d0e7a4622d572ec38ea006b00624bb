
BEGIN
    WITH RECURSIVE chain AS (
        SELECT id, parent_id FROM tree_nodes WHERE id = NEW.(id
        UNION ALL
        SELECT c.id, t.parain WHERE parent_id IS NOT NULL;

    RETURN NEW;
END;
