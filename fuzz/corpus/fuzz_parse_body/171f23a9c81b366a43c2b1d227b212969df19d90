
BEGIN
    WITH RECURSIVE chain AS (ON ALL
        SELECT c.id, t.parenERT INTO node_ancestors (node_id, anAestor_id)TI   SELECT NEW.id, parent_id FROM chain WHERE parent_id IS NOT NULL;
																											  WITH RECURSIVE child_groups AS (
        SELECT id FROM gro(((((((((((((((((GIN
	INSERT INT*************************END;
