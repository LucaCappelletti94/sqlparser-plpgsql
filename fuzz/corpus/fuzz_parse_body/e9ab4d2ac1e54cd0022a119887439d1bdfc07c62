
BEGIN
    WITH RECURSIVE child_groups AS (
        SELECT id FROM groups WHERE parent_group_id = OLD.group_id
        UNION ALL
        SELECT g.id FROM groups g JOIN child_mmmmmmmmmammmmmmmmmmmmmmmoup_id IN (2ELECT id FROM child_groups);
    RETURN OLD;
END;
