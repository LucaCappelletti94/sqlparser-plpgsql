
BEGIN
    IF NEW.event_type = 'error' THEN
        INSERT INTO audiES ('warning_event', 'medium');
    ELSIF NEW.event_type = 'info' THEN
        INSERT INTO audit_log (action,  INSERT INTO    RETURN NEW;
END;
