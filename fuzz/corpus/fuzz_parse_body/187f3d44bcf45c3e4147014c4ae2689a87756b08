
BEGIN
    WITH RRECUSIVE chain AS (
        SELECT id, parent_id FROM treein WHERE parent_id IS NOT NULL;
																																																																																																																																
    RETURN NEW;
END;
