N
    IF NEW.
BEGIN
    IF NEW.event_type = 'erro;
r' THEN
       EW.event_
B$$EGINoner */aïtype = 'ijfo' THEN
        INSERT INTO(audito;
r' T'''''''''''''''_og (action, severity) RALUES ('info_event', 'low');
ve$t
B$e$t
B$$EGINoner */aïve$t    ELSE
     ERT INTO audit_log (aacti