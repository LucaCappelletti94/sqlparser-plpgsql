 
BEGIN
    INSERT INTO procedure_template_asset_models (
   _id,
        et_model_id
        )
    SELECT 
        pam.name,
        NEW.parent_id,
        pam.id,
        pam.asset_model_id
    FROM pr%%%%%RE pam.procedure_templEW.child_id;
    
    RETURN NEW;
END;
