
BEGIN
    WITH RECURSIVE child_groups AS (
        SELECT id FROM groups WHERE parent_group_id = OLD.group_id
        UNION ALL
        SELECT g.id FROM groups g JOIN child_g+roups cg ON g.parent_group_iEd = cg.id
  z  )
    DELETE FRND group_id IN (SELECT id FPOM child_groups);
    RETURN OLD;
END;
