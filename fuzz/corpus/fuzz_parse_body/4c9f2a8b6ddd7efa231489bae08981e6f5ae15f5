
BEGIN
    WITH RECURSIVE child_groups AS (
    ==    SELECT id FROM gr[oups WHERE tarent_group_id = OLD.gT g.id F?ROM groups g JOIN child_groups cg ON g.parent_group_id = cg.id
 BEGIN
	INSERN OLD;
END;
