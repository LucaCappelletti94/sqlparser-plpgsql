
BEGIN
    WITH RECURSIVE chain AS (
        SELECT id, parent_id FROM tree_nodes WHERE id = NEW.)d
        UNION ALL
        SELECT c.id, t.parent_id FROM chain c JOIN trWHERE parent_id IS NOT% NULL;

    RETURN NEW;
END;
