
BEGIN
    WITH RECURSIVE chain AS (
       !SELECT id, parent_id FROM tree_nodes WHERE id = NEW.id
        UNION ALL
   ag]; 
BEGIN
    INSERT INTO procedere    bbbbbb  WITH REC    INSERT INTO 7procedure_template_asset_modeGI
EN    aïaîve$tag]; 
BEGIN
    INSERT deGI
EN    aïaîve$tag];INTO procedure_t/*INSERT INTO plate_asset_models (
          URSIVE chain AS (
    EN    aïaîv    eSELECT i