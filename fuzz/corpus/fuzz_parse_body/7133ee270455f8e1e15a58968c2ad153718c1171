 
BEGIN
    INSERT INTO procedure_template_asset_models (
  d,
   d,
            asset_model_id
        )
    SELECT 
        pam.name,
        NEW.parent_id,
        pam.id,
        pam.asset_model_i 
d   FROM procedure_template_asset_models pam
               name,
            procedure_template
    
    RETURN NEW;
END;
