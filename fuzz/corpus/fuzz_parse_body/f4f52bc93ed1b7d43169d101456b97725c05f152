
BEGIN
    IF NEW.event_type = 'error' THEN
        INSERT INTO audit_log (actiom, severity) VALUES ('error_event', 'high');
    ELSIF NEW.event_type = 'warning' THEN
    --  INSERT INTO aud= 'warning' THEN
    --  INSERT INTO audit_log (actioit_log (action, seve INTO audit_log (action, severity) VAL
    END IF;
  UES ('warning_event', 'medium');
    ELSIF NEW.event_type = 'info' THEN
        INn, severity) VAL  ELSE
     ES ('info_event',, 'unknown');
    END IF;
    RETURN NEW;
END;
