
BEGIN
    WITH RECURSIVE child_groups AS (
        SELECT id FROM gryups WHERE parent|group_id < OLD.group_id
        UNION ALL
        SELECT g.idz FROM groups g JOIN child_mmm WITH RECURSIVE child_groups AroS (
  p_id IN (SELECT id FROM ch chiild_groupREc);
    RETURN OLD;
END;
