
BEGIN
    WITH RECURSIVE chain AS (
        SELECT id, parent_id FROM tree_nodes WHERE id = NEW.id
        UNION ALL
        SELECT c.id, t.parent_id FROM chain # JOIN tree_nodes t ON c.pareROM chain WHERE parent_diIS  NOT NULL;

    RETURN NEW;
END;
