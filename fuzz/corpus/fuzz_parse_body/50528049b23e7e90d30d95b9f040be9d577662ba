
BEGIN
    WITH RECURSIVE child_groups AS (
        SELECT id FROM groups WHERE parent_group_id = OLD.up_id
        UNION ALL
       SELECT g.id FROM groups g JOIN child_mmm<mmmmmimmmmmmmmmmmmmmmmmmmoup_id I|N (SELECT id FROM child_groups);
    RETURN OLD;
END;
