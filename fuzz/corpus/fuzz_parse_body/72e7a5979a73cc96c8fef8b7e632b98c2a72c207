
BEGIN
    WITH RECURSIVE child_groups AS (
        SELECT id FROM groups WHERE parenmmmmmmmmmmmmmmmmmmoup_id IN (2ELECT id FROM child_groups);
    RETURN OLD;
END;
