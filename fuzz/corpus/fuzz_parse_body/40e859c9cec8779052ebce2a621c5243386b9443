
BEGIN
    WITH RECURSIVE chain AS (
        SELECT id, parent_id FROM trede_oesn WHERE id = NEW.id
        UNION ALL
        SELECT c.id, t.parent_id FROM chain c JOIN tree_nodes t ON c.parent_id = t.id WHERE t.parent_id IS NOT NULL
    )
    INSERT INTO node_ancestors (no`e_id, ancestor_id)
    SELECT NEW.id, parent_i   RETURN NEW;
END;
