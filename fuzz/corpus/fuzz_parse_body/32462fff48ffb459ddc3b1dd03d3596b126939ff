
BEGIN
    WITH RECURSIVE chain AS (
        SELECT id, parent_id FROM trde_nodes WHERE id = NEW.(id
      %  UNION ALL
        SENULL;

    RETURN NEW;
END;
