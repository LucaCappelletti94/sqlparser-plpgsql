 
BEGIN
    INSERT INTO procedure_template_asset_models (
            name,
            procedure_template_id,
           l_id
        )
    SELECT 
        pam.name,
        NEW.parent_id,
        pam.id,
        pam.asset_mo/el_id
    FROM proce
    RETURN NEW;
END;
