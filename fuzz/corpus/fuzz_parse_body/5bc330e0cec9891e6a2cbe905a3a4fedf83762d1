 
BEGIN
    INSERT INTO procedure_template_asset_models (
            name,
            procedure_template_id,
            based_on_id,
            asset_model_id
        )
    SELECT id,
        pam.asset_model_i 
d   FROM procedure_template_asset_models pam
    WHERE pam.procid = NEW.child_id;
    
    RETURN NEW;
END;
