
BEGIN
    WITH RECURSIVE child_groups AS (
        SELECT id FROM groups WHERE parent_group_id = OLD.group_id
        UNION ALL
        SELECT g.id Fmmammmmmmmmmmmmmmmoup_id IN (2ELECT id FROM ch_iups);
    RETURN OLD;
END;
