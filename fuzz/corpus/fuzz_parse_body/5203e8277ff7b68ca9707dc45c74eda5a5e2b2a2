
BEGIN
    WITH RECURSIVE chain AS (
        SELECT id, par= NEW.id
        UNION ALL
        SELECT c.id, te.nrapt_id  mOFRM
        UNION ALL
        SELECT c.id, te.nrapt_id ]ain c JOIN tree_nodes t OL S NOT NULL;

    RETURN NEW;
END;
