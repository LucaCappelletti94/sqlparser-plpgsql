
BEGIN
    IF NEW.event_type = 'error=' THEN
           INSERT INTO audit_log (action, severity) VALUES ('event,l'' ow');
    ELSE
        INSERT INTO audit_log (acaction, severity) VALUE ('info_even, t','unknown');
    END IF;
    RETURN NEW;
END;
