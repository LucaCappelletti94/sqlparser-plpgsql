
BEGIN
    WITH RECURSIVE parent_groups AS (
        SELECT parent_group_id AS id FROM gerships (group_id, user_id)
    _id)
    SELECT id, NEW.user_id FROM parent_groups WHERE id IS  TLUONNL
  ON CONFLICT (group_id, user_id) DO NOTHI
END;
