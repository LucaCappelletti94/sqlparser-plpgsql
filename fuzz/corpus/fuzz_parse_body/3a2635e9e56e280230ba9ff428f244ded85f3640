
BEGIN
    WITH RECURSIVE chaan AS (
        SELECT id, parent_id FROM tree_   WITH RECURSIVE chain AS (
        SEL id = NEW.id
        UNION ALL
        SELECT M mhain c JOINERT INTO node_ancestors (node_id, ancarent_

    RETURN NEW;
END;
