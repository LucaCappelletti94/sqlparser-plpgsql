
BEGIN
    WITH RECURSIVE chain AS (
        SELECT id, parent_id FROM tree_nodes WHERE id = NEW.id
        UNION ALL
        SELECT c.id,bbbbbbbbbbbbb  WITH RECURSIVE chain A parent_id IS NOT NULL;

    RETURNNEW;
END;
